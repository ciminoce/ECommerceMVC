@model ECommerceMVC.Web.ViewModels.Proveedor.ProveedorListViewModel
@{
    ViewBag.Title = "Delete";
}

<h2>Borrar Proveedor</h2>
<hr />
@using (Html.BeginForm("Delete", "Proveedores", FormMethod.Post))
{
    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
    @Html.AntiForgeryToken()

    <div class="form-group">
        @Html.LabelFor(m => m.NombreCompania)
        @Html.TextBoxFor(m => m.NombreCompania, new { @class = "form-control", @readonly = true })
    </div>
    <div class="row">
        <div class="col">

            <div class="form-group">
                @Html.LabelFor(m => m.Pais)
                @Html.TextBoxFor(m => m.Pais, new { @class = "form-control", @readonly = true })
            </div>

        </div>
        <div class="col">
            <div class="form-group">
                @Html.LabelFor(m => m.Ciudad)
                @Html.TextBoxFor(m => m.Ciudad, new { @class = "form-control", @readonly = true })
            </div>

        </div>
    </div>


    <div class="form-group">
        <button class="btn btn-danger" type="submit">Borrar</button>
        <a href="@Url.Action("Index", "Proveedores")" class="btn btn-success">Volver</a>

    </div>
}
